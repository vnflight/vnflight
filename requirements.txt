# vnflight has no required third-party packages: the CLI and the bridge run on
# the standard library.  MCP mode (`vnflight.py mcp`) needs the mcp package.
# mcp 2.x dropped the Server.list_tools/call_tool decorators the server uses.
mcp>=1.26,<2
