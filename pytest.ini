[pytest]
testpaths =
    tests
# tests/ is self-contained (tests/test_shipped_tree_is_self_contained.py);
# the development repository keeps its game/SDK/mod/harness tests under
# tests/local/, which is not staged.
